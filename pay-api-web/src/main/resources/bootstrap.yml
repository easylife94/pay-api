spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: pay-config-server
      profile: pay-api

eureka:
  instance:
    hostname: pay-api
  client:
    service-url:
      defaultZone: http://pay-eureka-server:8761/eureka

